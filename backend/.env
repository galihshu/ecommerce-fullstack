# Server Configuration
PORT=8080
HOST=localhost

# Database Configuration (PostgreSQL Standalone)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=1337
DB_NAME=db_ecommerce
DB_SSLMODE=disable

# Redis Configuration (Optional - akan skip jika tidak ada)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=168h

# CORS Configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# Environment
ENV=development
